# secrets — this repo is public, never commit real credentials
.env
*.pem
*.key

# python
__pycache__/
*.pyc
.venv/
venv/
*.egg-info/

# eval output — only curated example reports get committed, rest is regenerable
eval/reports/*
!eval/reports/.gitkeep
!eval/reports/examples/

# local smoke-test logs — real audit schema lands at Milestone 2, this is throwaway
integration/razorpay_test_mode/webhook_smoke_test_log.jsonl

# os
.DS_Store

# generated report -- a committed snapshot lives in eval/reports/examples/
eval/reports/report.html
# generated live loop audit -- snapshot in examples/
eval/reports/live_webhook_decisions.jsonl

# personal interview prep, not submission material
docs/interview_prep.md
